import Container from '@/components/ui/Container';
import React from 'react';

const Products = () => {
  return (
    <Container>
      <section className="container">
        <h1 className="text-dark-2 mt-10 text-center text-2xl font-bold uppercase tracking-wider">
          Providers <span className="lowercase tracking-normal"> (ssr)</span>
        </h1>
      </section>
    </Container>
  );
};

export default Products;
